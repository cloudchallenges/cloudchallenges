---

---

<section class="relative py-16 md:py-24">
    <div class="container mx-auto px-4">
        <!-- Heading -->
        <div class="mx-auto mb-12 max-w-2xl text-center">
            <h2 class="mb-4 text-3xl font-bold tracking-tight md:text-4xl">
                How it works
            </h2>
        </div>

        <!-- Steps -->
        <div class="mx-auto max-w-5xl">
            <div class="grid gap-8 md:grid-cols-3">
                <!-- Step 1 -->
                <div class="relative flex flex-col items-center text-center">
                    <!-- Step number -->
                    <div
                        class="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-primary text-xl font-bold text-primary-foreground"
                    >
                        1
                    </div>
                    <!-- Connector line (hidden on mobile, visible on desktop) -->
                    <div
                        class="absolute left-[calc(50%+32px)] top-6 hidden h-0.5 w-[calc(100%-64px)] bg-border md:block"
                        aria-hidden="true"
                    >
                    </div>
                    <h3 class="mb-2 text-lg font-semibold">
                        Get the challenge
                    </h3>
                    <p class="text-sm text-muted-foreground">
                        Read the brief, constraints, and sample inputs. You
                        won't get hand-holding, you'll get a real problem.
                    </p>
                </div>

                <!-- Step 2 -->
                <div class="relative flex flex-col items-center text-center">
                    <div
                        class="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-primary text-xl font-bold text-primary-foreground"
                    >
                        2
                    </div>
                    <div
                        class="absolute left-[calc(50%+32px)] top-6 hidden h-0.5 w-[calc(100%-64px)] bg-border md:block"
                        aria-hidden="true"
                    >
                    </div>
                    <h3 class="mb-2 text-lg font-semibold">Design & build</h3>
                    <p class="text-sm text-muted-foreground">
                        Use Terraform, CI/CD, and cloud services to design a
                        reproducible system. Document decisions and justify
                        trade-offs.
                    </p>
                </div>

                <!-- Step 3 -->
                <div class="flex flex-col items-center text-center">
                    <div
                        class="mb-4 flex h-12 w-12 items-center justify-center rounded-full bg-primary text-xl font-bold text-primary-foreground"
                    >
                        3
                    </div>
                    <h3 class="mb-2 text-lg font-semibold">
                        Validate & showcase
                    </h3>
                    <p class="text-sm text-muted-foreground">
                        Use the end-state checklist to validate your work. Add
                        your solution and notes to your portfolio.
                    </p>
                </div>
            </div>

            <!-- Microcopy -->
            <p class="mt-10 text-center text-sm text-muted-foreground">
                Each challenge is designed for a focused 2 to 3 day effort. You
                deploy in your own cloud account. We keep costs modest and
                free-tier friendly.
            </p>
        </div>
    </div>
</section>
